- hosts: platform
  tasks:
    - import_role:
        name: azure-aks
        tasks_from: platform-regions-load

    - include_role:
        name: azure-aks
      loop: "{{ platform_regions }}"
      loop_control:
        loop_var: platform_region
