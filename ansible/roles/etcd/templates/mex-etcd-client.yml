apiVersion: v1
kind: Service
metadata:
  name: "{{ etcd_cluster_client }}"
spec:
  ports:
  - port: 2379
    name: client
  - port: 2380
    name: peer
  selector:
    component: "{{ etcd_cluster_name }}"
