---
- debug:
    msg: "Backing up etcd data from cluster '{{ k8s_cluster_name }}' (region: '{{ region }}')"

- import_role:
    name: k8s_setup

- import_role:
    name: controller
    tasks_from: etcd-backup
