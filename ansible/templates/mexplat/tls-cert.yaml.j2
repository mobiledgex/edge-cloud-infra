apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: {{ name }}
spec:
  commonName: {{ fqdn }}
  secretName: {{ secret_name }}
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
