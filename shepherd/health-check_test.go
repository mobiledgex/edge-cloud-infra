package main

import "testing"

func TestHealthCheck(t *testing.T) {
	// TODO - see below
	// Test by adding a failing Health Check and failing RootLb
	// Afterwards clear RootLB failure
	// Make sure the alert is still there
	// Clear HealthCheck and make sure that at that point alert is cleared

}
